<!DOCTYPE html>
<html lang="en">

<head>
    <meta charset="UTF-8">
    <meta name="viewport" content="width=device-width, initial-scale=1.0">

    <script src="https://cdn.jsdelivr.net/npm/@tailwindcss/browser@4"></script>
    <link rel="stylesheet" href="/stylesheets/style.css">
    <title>Project</title>
</head>

<body>
    <div class="w-full h-screen bg-zinc-900 p-7 text-white">

        <div class="nav mb-10">
            <a class="text-blue-500" href="/read">📚 Read Users</a>
        </div>

        <h1 class="text-3xl tracking-tighter mb-3">Create User</h1>
        <form action="/update/<%=user._id%>" method="post">
            <input type="text" placeholder="<%=user.name%>" name="name" value="<%=user.name%>"
                class="px-5 py-2 text-yellow-200 border-2 ml-1 border-zinc-800 rounded-lg bg-transparent outline-none">
            <input type="text" placeholder="<%=user.email%>" name="email" value="<%=user.email%>"
                class="px-5 py-2 border-2 ml-1 text-yellow-200 border-zinc-800 rounded-lg bg-transparent outline-none">
            <input type="text" placeholder="<%=user.image%>" name="image" value="<%=user.image%>"
                class="px-5 py-2 border-2 ml-1 text-yellow-200 border-zinc-800 rounded-lg bg-transparent outline-none">
            <input type="submit" value="Update User"
                class="px-5 py-2 border-2 border-orange-400 mt-3 rounded-lg bg-orange-500 outline-none">
        </form>
    </div>
</body>

</html>